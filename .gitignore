# Dependencies — regenerated from package.json via `npm install`
node_modules/

# Claude Code local session state
.claude/

# Test runner caches
.vitest/
coverage/

# Build / temp
dist/
build/
.cache/
*.log
npm-debug.log*

# Capacitor build output (regenerated by npm run build:www)
www/

# Android build output (the rest of android/ should be committed once
# `npx cap add android` has been run and customised).
android/build/
android/app/build/
android/.gradle/
android/.idea/
android/captures/
android/local.properties
android/keystore.properties
*.keystore
*.jks
# Regenerated by `npx cap sync` from www/. Don't commit ~100 MB of
# music copies — `npm run android:sync` rebuilds this dir from the
# canonical sources before each Gradle build.
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Local env overrides
.env
.env.local
